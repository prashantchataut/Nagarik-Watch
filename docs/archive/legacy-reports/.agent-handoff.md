# Nagarik Watch — Session Handoff

## Identity

**Nagarik Watch (नागरिक वाच)** — Devanagari-first Nepali news portal. Payload 3.85.1 CMS + Next.js 15 public site. Monorepo (pnpm workspaces). Postgres 16. Civic Crimson palette. Mukta/Noto Sans Devanagari/Inter fonts.

## State

- **Phase 1 Task 1.1** — 5 collections written (Categories, Articles, Authors, Tags, Media) but NOT registered in payload.config.ts. Seed script NOT created. Build WILL FAIL `pnpm typecheck`.
- Phase 0: green (build, tokens, fonts, tests, CI all pass).
- 93 files committed, git remote set.

## Immediate Next Action

**Task 1.1 completion — two steps:**

1. Register all 5 collections in `apps/admin/src/payload.config.ts` (currently only `Users`)
2. Create `apps/admin/src/seed/index.ts` that imports seed data from `apps/web/lib/content/seed/` and creates categories + articles via Payload Local API

Verify: `pnpm typecheck && pnpm lint && pnpm test && pnpm build`

## Constraints (read AGENT.md full)

- **No #000, #fff, gradient text, glassmorphism, side-stripe borders, modal as default** — Civic Crimson oklch(0.55 0.18 25) primary
- Bilingual fields: Ne-suffix (Nepali), En-suffix (English) — field-level, not document-level
- BS date: 2026-06-19 = असार ५, २०८३
- No comments in code, no `any`, prefer `type` over `interface`
- `.opencode/` and `graphify-out/` are gitignored — do not commit

## Context Sources (in order)

1. **AGENT.md** — full project instructions, skills, memory, architecture (this repo)
2. **docs/** — content-model.md, architecture.md, phase-1-tasks.md, editorial-workflow.md
3. **Mempalace** — `python -m mempalace search "X" --wing nagarik-watch` (479 drawers mined)
4. **Graphify** — `graphify-out/graph.json` if exists; else `graphify . --mode deep --no-viz`
5. **Existing collections** — `apps/admin/src/collections/Users.ts` (pattern to follow)
6. **Seed data** — `apps/web/lib/content/seed/*.ts` (articles, categories, authors, tags, media)

## Active Files (read before editing)

- `apps/admin/src/payload.config.ts` — register collections here
- `apps/admin/src/collections/{Categories,Articles,Authors,Tags,Media}.ts` — written, need review
- `apps/admin/src/components/BrandMark.tsx` — admin brand component
- `apps/web/lib/content/seed/` — seed data payloads to import

## Skills Always On

karpathy-guidelines, anti-slop, agentic-engineering, doubt-driven-development, verification-before-completion, incremental-implementation, caveman, council

## Mempalace CLI (MCP broken)

```bash
python -m mempalace status
python -m mempalace search "query" --wing nagarik-watch
python -m mempalace add --wing nagarik-watch --room X --content "..."
```

## After Task 1.1

Sequence: Task 1.2 (query layer) → 1.3 (homepage) → 1.4 (category page) → 1.5 (article page) → 1.6–1.14 per docs/phase-1-tasks.md
