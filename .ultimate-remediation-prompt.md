# NAGARIK WATCH — ULTIMATE COMPREHENSIVE REMEDIATION & COMPLETION PROMPT

> **TO THE AI AGENT:** You have ZERO context of this project. I have provided a zip file containing the entire monorepo. Your job is to:
> 1. First, thoroughly scan and understand the ENTIRE codebase
> 2. Then, execute ALL the tasks below in order
> 3. Along the way, discover ADDITIONAL issues I haven't listed
> 4. Leave NOTHING half-done. No "Coming soon", no "TODO", no stubs, no mocks.
> 5. When you finish, generate a continuation prompt for what couldn't be done.
>
> **THIS IS PRODUCTION. NOT A DEMO. NOT A PROTOTYPE. THE CLIENT SEES THIS TOMORROW.**

---

## ⚠️ CRITICAL RULES

1. **USE SKILLS ON EVERY TASK.** The available skills are listed at the bottom. You MUST load the relevant skill before each task. This is non-negotiable.
2. **NO HALF MEASURES.** Every page must be functional. Every button must work. Every data source must be real.
3. **NO AI-SLOP.** Don't write generic copy. Don't add fake testimonials. Don't use "Empower your business" language. Every design choice must be intentional.
4. **SCAN FIRST.** Before touching ANY code, spend 30+ minutes reading every critical file in the project to understand architecture.
5. **FIND WHAT I MISSED.** At the end, produce a list of EVERY issue you found that I didn't mention.
6. **VERIFY EVERYTHING.** Run `pnpm typecheck`, `pnpm lint`, `pnpm test` before claiming completion.
7. **COMMIT FREQUENTLY.** After each logical task, commit with descriptive messages.

---

## PHASE 0: EMERGENCY SECURITY — DO FIRST

### Task 0.1: Rotate ALL secrets

The `.env` file is COMMITTED TO GIT. This is catastrophic.

1. Add `.env` to `.gitignore` if not already there
2. `git rm --cached .env` to remove it from tracking
3. Generate NEW values for ALL of these:
   - `AUTH_SECRET` (generate via `openssl rand -base64 32`)
   - `BETTER_AUTH_SECRET` (same value, deduplicate)
   - `NEWSROOM_SUPERADMIN_PASSWORD`
   - `NEWSROOM_ADMIN_PASSWORD`
   - `TURNSTILE_SECRET_KEY`
   - `TURNSTILE_SITE_KEY`
   - `PAYLOAD_SECRET`
4. Remove `VERCEL_TOKEN` from `.env` — this should NEVER be in env files
5. Remove duplicate `NEXT_PUBLIC_SITE_URL` entry (appears twice in `.env`)

### Task 0.2: Fix production URL fallbacks

Every single `http://localhost:3000` default is a production bug. Find and fix ALL of them:
- `apps/web/lib/site.ts:6` — fallback for `NEXT_PUBLIC_SITE_URL`
- `apps/web/lib/auth/index.ts:67` — `auth.origin` fallback
- `apps/web/lib/security/origin.ts:29` — CORS origin whitelist
- `apps/admin/src/payload.config.ts:41` — server URL
- `apps/web/app/admin/articles/page.tsx:68` — PAYLOAD_ADMIN_URL fallback

Change defaults to crash at boot with a clear error message. Use `process.env.NEXT_PUBLIC_SITE_URL || (() => { throw new Error('NEXT_PUBLIC_SITE_URL required') })()` pattern.

### Task 0.3: Fix weak dev fallback secret

`apps/web/lib/auth/index.ts:40` has `'dev-only-secret-change-me-please-32-chars-minimum'` hardcoded. If env var is unset in production, this silently signs sessions with a weak key. Make it throw in production.

---

## PHASE 1: DATABASE & AUTH — MAKE THINGS WORK

### Task 1.1: Fix PostgreSQL / Database setup

The admin login is broken because PostgreSQL is not running. The `.env` has `DATABASE_URL` pointing to a local Postgres instance.

**SKILL: Use `devops-engineer`**

Options (pick the best one):
1. **Remove `DATABASE_URL` from `.env`** so the fallback to PGlite (in-memory Postgres WASM) kicks in for development — this is the fastest path to working auth
2. **OR** set up PostgreSQL properly with Docker
3. **OR** configure a proper production database connection

**Critical:** After fixing the DB:
- Verify `BETTER_AUTH_URL` is in `.env` and `turbo.json` `globalEnv`
- Test the admin login flow: go to `/admin/login`, enter superadmin credentials, confirm redirect to `/admin/dashboard`
- Verify session persists across page reloads

### Task 1.2: Fix seed boot accounts

The `seedBootAccounts()` function in `apps/web/lib/auth/index.ts` silently fails when DB is down. After fixing the DB connection:
- Verify seed accounts are created on first boot
- Test login with `superadmin@nagarikwatch.com` / `nagarikwatch@superadmin_`
- Test login with `admin@nagarikwatch.com` / `nagarikwatch@admin_`
- Verify proper role assignment (`super_admin` and `admin`)

### Task 1.3: Fix Better Auth configuration

- Add `BETTER_AUTH_URL` to `.env` if missing
- Add `BETTER_AUTH_URL` to `turbo.json` `globalEnv` array
- Verify the auth catch-all route at `/api/auth/[...all]` works

---

## PHASE 2: COMPLETE THE ADMIN PANEL

The admin panel has 23 pages. Only 5 are functional. Fix ALL of them.

**SKILL: Use `newsroom-cms-architecture-skill`, `form-expert`, `fullstack-guardian`**

### Task 2.1: Admin categories page — Wire CRUD

`apps/web/app/admin/categories/page.tsx` — "New Category" button is disabled with "Payload config needed"

**Fix:** Wire the category management to actually work:
- Connect to the JSON store OR create a proper category persistence layer
- Enable the create/edit/delete buttons
- Add form validation (name required, slug auto-generated)
- Show real categories in the list
- Extend seed categories to include: **literature (साहित्य)**, technology (प्रविधि), health (स्वास्थ्य), education (शिक्षा), interview (अन्तर्वार्ता), photo-story (फोटो फिचर), video (भिडियो), opinion (विचार) — more diverse categories

### Task 2.2: Admin tags page — Wire CRUD

`apps/web/app/admin/tags/page.tsx` — "New Tag" disabled

**Fix:** Same as categories — wire full CRUD

### Task 2.3: Admin authors page — Wire CRUD

`apps/web/app/admin/authors/page.tsx` — "New Author" disabled

**Fix:** Wire full author CRUD with:
- Name, slug, role (staff/columnist/contributor/wire)
- Photo upload (store locally or to memory)
- Bio, email, social links
- Active/inactive toggle

### Task 2.4: Admin media page — Wire upload

`apps/web/app/admin/media/page.tsx` — Upload is a "styled placeholder"

**Fix:** Implement workable media upload:
- In development: store to `public/uploads/` or `data/media/`
- Show uploaded images in a grid
- Add alt text, caption, credit fields per image
- Show image preview on click

### Task 2.5: Admin polls page — Wire persistence

`apps/web/app/admin/polls/page.tsx` — In-memory only, lost on refresh

**Fix:** Store polls to a local JSON file or the article store:
- Create/edit/delete polls
- Show poll results
- Wire the public poll display on articles

### Task 2.6: Admin newsletter page — Wire send button

`apps/web/app/admin/newsletter/page.tsx` — Send button is disabled

**Fix:** Enable the send functionality:
- When no `NEWSLETTER_API_KEY` is set, save drafts locally to a queue file
- Enable sending via a local fallback
- Show subscriber count from the store

### Task 2.7: Admin provinces page — Wire editor

`apps/web/app/admin/provinces/page.tsx` — Province editor button disabled

**Fix:** Wire province content management

### Task 2.8: Admin live-blogs page — Wire creation

`apps/web/app/admin/live-blogs/page.tsx` — Creates disabled, empty state

**Fix:** Wire live blog creation and management

### Task 2.9: Admin submissions page — Wire the queue

`apps/web/app/admin/submissions/page.tsx` — Empty queue, no persistence

**Fix:** Wire the reader submission queue to actually show submissions

### Task 2.10: Admin audit-log page — Wire the log

`apps/web/app/admin/audit-log/page.tsx` — Empty table, writes not wired

**Fix:** Wire audit log recording and display

### Task 2.11: Admin ads page — Wire management

`apps/web/app/admin/ads/page.tsx` — Read-only inventory reference

**Fix:** Add ability to toggle ad placements on/off, set custom content for house ads

### Task 2.12: Admin settings page — Wire editing

`apps/web/app/admin/settings/page.tsx` — Read-only env var display

**Fix:** Allow editing of key settings (publication name, tagline, social links, etc.)

### Task 2.13: Admin roles page — Wire editing

`apps/web/app/admin/roles/page.tsx` — Read-only view

**Fix:** Allow super_admin to manage roles and permissions

### Task 2.14: Admin users page — Wire invite

`apps/web/app/admin/users/page.tsx` — Only shows current user, invite disabled

**Fix:** Wire user listing and invite functionality

---

## PHASE 3: JOURNALIST LOGIN — SEPARATE FROM ADMIN

**SKILL: Use `secure-code-guardian`, `form-expert`, `nextjs-developer`**

The user wants a **separate journalist login** that is NOT the admin panel. This should be a dedicated login experience for journalists/contributors with a simplified interface.

### Task 3.1: Create journalist login page at `/journalist/login`

- Create `apps/web/app/[locale]/journalist/login/page.tsx`
- Create `apps/web/app/[locale]/journalist/login/JournalistLoginForm.tsx`
- Similar to admin login but with journalist branding
- After login, redirect to a simplified journalist dashboard at `/journalist/dashboard`

### Task 3.2: Create journalist dashboard at `/journalist/dashboard`

Simple interface for journalists:
- "My Articles" list (filtered by `assignedTo` / author)
- "Create New Article" button → simplified article editor
- "My Profile" section
- Notifications/alerts
- Reading stats for their articles

### Task 3.3: Create journalist-specific article editor

A SIMPLIFIED version of the admin ArticleEditor:
- Remove complex workflow stages (journalist only sees: Draft → Submit)
- Remove SEO fields, premium toggle, sensitive flag
- Keep: title, body, category, tags, hero image
- Remove admin-only fields

### Task 3.4: Protect journalist routes

- Server-side session check for `journalist` role and above
- If reader → redirect to main site
- If admin → keep admin access

---

## PHASE 4: USER LOGIN & READER ACCOUNTS

**SKILL: Use `secure-code-guardian`, `signup`, `onboarding`**

Better Auth exists but login/registration is not fully wired for readers.

### Task 4.1: Create reader login page at `/login`

- Create `apps/web/app/[locale]/login/page.tsx`
- Create `apps/web/app/[locale]/login/LoginForm.tsx`
- Simple email + password login
- "Create account" link
- "Forgot password" flow (wire to Better Auth's built-in forgot password)

### Task 4.2: Create reader registration page at `/register`

- Create `apps/web/app/[locale]/register/page.tsx`
- Create `apps/web/app/[locale]/register/RegisterForm.tsx`
- Fields: name, email, password, confirm password
- POST to `/api/auth/sign-up/email`
- After registration: redirect to `/welcome` or home

### Task 4.3: Create reader profile page at `/profile`

- Shows reading history (stored in localStorage)
- Shows bookmarked articles
- Notification preferences
- Dark mode preference
- Language preference
- Sign out button

### Task 4.4: Wire saved/bookmark functionality

The site has bookmarks stored in localStorage but no UI to view them properly:
- Create `/saved` page showing bookmarked articles
- Wire the bookmark toggle on article cards
- Show saved count in the header

---

## PHASE 5: ADMIN HIERARCHY & ROLE MANAGEMENT

**SKILL: Use `form-expert`, `fullstack-guardian`, `secure-code-guardian`**

### Task 5.1: Audit existing roles

The codebase has 19+ roles defined. Create a clear hierarchy:

```
super_admin           → Full system access
admin                 → All admin except system config
publisher             → Can publish any content
editor_in_chief       → Editorial oversight
managing_editor       → Day-to-day editorial management
section_editor        → Section-specific editing
assistant_editor      → Supports section editors
sub_editor            → Copy editing, fact-checking
copy_editor           → Copy editing only
fact_checker          → Fact-checking only
seo_manager           → SEO metadata
moderator             → Comment moderation
ad_manager            → Ad management
analyst               → Analytics & data
photo_video_editor    → Media management
journalist            → Article creation
contributor           → Can submit articles
reader                → Can read and comment
```

### Task 5.2: Create role management UI

- Admin page at `/admin/roles` should allow editing permissions
- Show which role can access which admin page
- Show which role can perform which action (create/edit/publish/delete)

### Task 5.3: Create user management UI

- Admin page at `/admin/users` should show ALL users
- Ability to create users with role assignment
- Ability to change user roles
- Ability to deactivate users
- Search/filter users

### Task 5.4: Create journalist management

- Admin page to manage journalists specifically
- Assign journalists to sections/categories
- View journalist article counts and stats
- Journalist activity log

---

## PHASE 6: LIVE ADMIN PANEL WITH BAYESIAN & LTV

**SKILL: Use `analytics`, `ab-testing`, `marketing-psychology`, `caching-strategies`**

### Task 6.1: Create analytics dashboard

Create a live admin panel at `/admin/analytics` or `/admin/live`:
- Real-time visitor count (using page view events)
- Article performance (views, read time, shares)
- Top stories today/this week/this month
- Traffic sources (direct/social/search/referral)
- Reader geography (province/country)

### Task 6.2: Wire the recommendation engine

The `packages/db/src/recommend.ts` has a 320-line recommendation engine that is NEVER used.

**Fix:** Wire it into the article page and homepage:
- Article page: "Related stories" section using the content-based engine
- Homepage: "Recommended for you" section using session-based + freshness
- Wire the `continueReading()` function for logged-in users

### Task 6.3: Wire the trending engine

`packages/db/src/trending.ts` has velocity/burst/trending detection that is NEVER used.

**Fix:** Wire it into:
- Trending section on homepage
- Trending sidebar widget
- Trending admin dashboard

### Task 6.4: Create A/B testing framework

Build a simple A/B testing system:
- Define test variants in admin
- Assign users randomly to variants
- Track conversion metrics
- Report results

### Task 6.5: Create LTV / engagement scoring

Build a reader value scoring system:
- Track: page views, read time, shares, comments, return frequency
- Compute a "reader score" for each user
- Show in admin dashboard

---

## PHASE 7: LIVE DATA — MAKE IT REAL

**SKILL: Use `live-data-widget-integration-skill`, `monitoring-expert`, `caching-strategies`**

### Task 7.1: Consolidate two live data codebases

There are TWO live data systems:
- `apps/web/lib/live-data.ts` (old, all mock)
- `apps/web/lib/live/` (new: real.ts, mock.ts, sports.ts, health.ts)

**Fix:** Remove the old `live-data.ts`. Ensure everything uses the new `lib/live/` system.

### Task 7.2: Wire actual data providers

Current status of ALL 13 providers:

| Provider | Real? | API Key? | Action |
|----------|-------|----------|--------|
| Weather (Open-Meteo) | ✅ Yes, free | No | Remove `mock()` fallback. Always show real data |
| AQI (Open-Meteo) | ✅ Yes, free | No | Remove `mock()` fallback. Always show real data |
| Forex (NRB) | ✅ Yes, free | No | Remove `mock()` fallback. Always show real data |
| NEPSE | ❌ Flaky (regex scrapes HTML) | No | Build a proper API integration. Scrape is fragile |
| Gold/Silver | ❌ Always mock | Yes | Find a free source or build a manual update mechanism in admin |
| Football | ✅ Conditional | Yes | If `FOOTBALL_API_KEY` unset, show "scores" from a free source |
| Cricket | ✅ Conditional | Yes | Same as football |
| Election | ❌ Always mock | Yes | Build an admin-updatable election data mechanism |
| Exam Results | ❌ Always mock | Yes | Build admin-updatable exam results |
| Disaster Alerts | ❌ Always mock | Yes | Build admin-updatable disaster alerts |
| Parliament Live | ❌ Always mock | Yes | Build admin-updatable parliament data |
| YouTube Live | ❌ Always mock | Yes | If `YOUTUBE_API_KEY` unset, allow admin to manually set stream URL |

**For every provider that stays mock:** Create an admin panel to UPDATE the mock data manually. The data should NOT be hardcoded.

### Task 7.3: Fix health check for Open-Meteo

`apps/web/lib/live/health.ts` marks Open-Meteo as "unhealthy" if `WEATHER_API_KEY` is not set. But Open-Meteo DOES NOT NEED an API key.

**Fix:** Remove the API key requirement for Open-Meteo. Weather and AQI should show as healthy when they're working.

### Task 7.4: Add real-time updates

Live data is currently server-rendered only (no client updates).
- Add client-side polling for: NEPSE, weather, AQI
- Poll every 60-300 seconds depending on the data type
- Show "last updated" timestamp
- Add loading states during refresh

### Task 7.5: Add cache limits

The in-process cache (`Map<string, CacheEntry>`) in `real.ts` has NO memory limit. Add:
- Max 100 entries
- LRU eviction
- 5-min default TTL per provider

---

## PHASE 8: CONTENT & PUBLICATION SETUP

### Task 8.1: Set publication credentials

**Fix ALL empty publication fields in `.env`:**
- `NEXT_PUBLIC_PUBLICATION_LEGAL_NAME` — set to something real
- `NEXT_PUBLIC_EDITOR_IN_CHIEF` — set to a name
- `NEXT_PUBLIC_DOIB_NUMBER` — set to a placeholder registration number
- `NEXT_PUBLIC_PUBLICATION_PHONE` — set to a contact number
- `NEXT_PUBLIC_PUBLICATION_ADDRESS` — set to an address
- `NEXT_PUBLIC_ADMIN_EMAIL` — set to admin email

### Task 8.2: Create seed articles with real content

The article store is EMPTY (`data/articles.json` has `"articles":[]`).

**Fix:** Create at least 30-40 real-looking articles spanning categories:
- Politics (राजनीति): 5 articles
- Society (समाज): 5 articles
- Business (बजार): 4 articles
- Sports (खेलकुद): 4 articles (include FIFA World Cup 2026)
- Entertainment (मनोरञ्जन): 3 articles
- World (विश्व): 3 articles
- Opinion (विचार): 3 articles
- Technology (प्रविधि): 3 articles
- Health (स्वास्थ्य): 3 articles
- Literature (साहित्य): 2 articles
- Interview (अन्तर्वार्ता): 2 articles

Each article should have:
- Nepali title and body
- English title (transliteration or translation)
- Category assignment
- Author assignment
- Realistic content about current (June 2026) Nepal-relevant topics
- Hero image placeholder
- Featured state (lead for top 2, secondary for next 4)
- At least 3 articles should be marked `premium: true` so the paywall can be tested

### Task 8.3: Connect Payload CMS (optional but recommended)

The `PAYLOAD_CONTENT_SOURCE` env var is commented out. If you can:
- Set `PAYLOAD_CONTENT_SOURCE=payload`
- Run Payload CMS migrations
- Verify content flows from Payload to the frontend
- Set up revalidation webhooks

If not possible, ensure the JSON store path is production-ready:
- Remove JSON store writes to `process.cwd() + '/data/'` (read-only on Vercel)
- Add `ARTICLES_STORE_URL` env var for blob storage fallback
- Document it in `.env.example`

### Task 8.4: Fix Unsplash hotlinking

Seed images use direct Unsplash URLs. This is against Unsplash terms for production use and will break.

**Fix:**
- Download seed images locally to `public/images/seed/`
- Update seed scripts to use local paths
- Or use a proper image hosting solution

---

## PHASE 9: UI/UX OVERHAUL

**SKILL: Use `impeccable`, `design-anti-slop`, `tailwind-css`, `accessibility-audit`**

### Task 9.1: Fix the logo area text

The current `<Logo>` component renders THREE lines:
```
नागरिक वाच
NAGARIK WATCH
PUBLIC INTEREST NEWS
```

The third line "PUBLIC INTEREST NEWS" looks like a generic tagline. The user perceives this as "Ello, a Nepali news portal" style branding.

**Fix:** Either:
- Remove the third line entirely
- Or replace with ONLY the Nepali tagline: "सत्यापित समाचार, स्पष्ट सन्दर्भ"
- Make the logo cleaner and more professional

### Task 9.2: Fix theme/locale interaction

The user reports that switching language toggles dark mode. While there's NO direct code coupling, the issue might be:
- Page navigation causes a flash or re-render
- The inline theme script in layout.tsx runs on every navigation

**Debug and fix:**
- Ensure theme persists correctly when navigating between locales
- Add a `matchMedia` listener for system preference changes
- Verify no flash occurs on page transitions
- Test: switch from Nepali to English, verify theme stays the same
- Test: switch from English to Nepali, verify theme stays the same
- Test: toggle dark mode, switch language, verify dark mode persists

### Task 9.3: Remove the `.` at the end of logo

In Logo.tsx, check for trailing periods or punctuation in text rendering and clean it up.

### Task 9.4: Redesign the UI to be anti-slop

Use `design-anti-slop` to audit the ENTIRE site:

**Pages to audit:**
- Homepage — is it a generic news layout? Make it ownable
- Article page — is the reading experience good?
- Category pages — are they unique?
- Admin panel — does it look professional?
- Footer — is it complete?

**Fix:**
- No card-within-card patterns
- No purple/pink gradients
- No generic icon+text cards
- Original layout decisions, not templates
- Intentional color use, not defaults
- Proper typographic hierarchy

### Task 9.5: Add missing categories

The user specifically wants LITERATURE (साहित्य) as a category. Current categories are:
- politics, society, business, sports, entertainment, world, opinion, diaspora

**Add these categories:**
- literature (साहित्य) — NEW
- technology (प्रविधि) — NEW
- health (स्वास्थ्य) — NEW
- education (शिक्षा) — NEW
- interview (अन्तर्वार्ता) — NEW
- photo-story (फोटो फिचर) — NEW
- video (भिडियो) — NEW

Update navigation to include key categories.

### Task 9.6: Fix mobile nav issues

- Mobile nav language toggle renders as a styled list item, not a link/button — fix consistency
- Province mega menu hover behavior may cause accidental closures — fix

### Task 9.7: Fix footer issues

- `/opinion` link in footer leads to 404 — fix
- `/fact-check` link in footer leads to 404 — fix or remove
- Add missing essential footer links (privacy, terms, contact, about)

---

## PHASE 10: PAYWALL & PREMIUM CONTENT

**SKILL: Use `paywalls`, `pricing`, `signup`**

### Task 10.1: Wire the premium field

The `premium` field exists on articles but is NEVER checked on the frontend.

**Fix:** Add premium gating:
- Check `article.premium` on the article page
- If premium and user is not a paying subscriber:
  - Show first 3 paragraphs
  - Show a "Subscribe to read more" paywall overlay
  - Add a `ContinueReading` component with subscription CTA

### Task 10.2: Create subscription UI

- Create a `/membership` or `/subscribe` page
- Show subscription tiers (Monthly/Yearly)
- Even if payment isn't wired, show the UI and intent
- Create a subscription check for the article paywall

### Task 10.3: Mark premium articles in UI

- Show a "Premium" badge on premium article cards
- Show a lock icon on premium articles in listings
- Grey out premium article preview for non-subscribers

---

## PHASE 11: ADVERTISEMENT — WIRE REAL ADS

**SKILL: Use `analytics`, `cro`**

### Task 11.1: Wire ad tracker API

The `AdTracker` component posts to `/api/ads/event` — check if this route exists. If not, create it.

### Task 11.2: Wire ad network mode

- Add `NEXT_PUBLIC_AD_NETWORK` and `NEXT_PUBLIC_ADSENSE_CLIENT` to `.env` with placeholder values
- Enable `network` mode in development to test ad containers
- Ensure ad slots are CLS-safe (fixed dimensions)

### Task 11.3: Connect real ad network or house ads

If no ad network is available:
- Create an admin panel to upload house ad creative (images with links)
- Allow advertisers to submit through the advertise page
- Show real house ads instead of "Advertise here" placeholders

---

## PHASE 12: READER SUBMISSIONS / SPA / PSA

**SKILL: Use `moderation-and-trust-safety-skill`, `form-expert`**

### Task 12.1: Create reader submission form

Currently there is NO form — just an informational workflow diagram.

**Fix:** Create a real submission form at `/submit-story`:
- Fields: Name (optional), Email, Phone (optional), Story type (tip/document/photo/video), Description, Evidence upload, Consent checkbox, Anonymity request checkbox
- POST to `/api/submissions`
- Store submissions locally (JSON file or in-memory for dev)
- Show confirmation message

### Task 12.2: Create submissions API

Create `apps/web/app/api/submissions/route.ts`:
- POST handler to accept submissions
- Rate limiting
- Validation
- Storage to JSON file

### Task 12.3: Wire admin submissions queue

The admin `/admin/submissions` page should show REAL submissions from the API:
- List with status filters (new/in_review/accepted/rejected)
- Click to view submission details
- Accept/reject buttons
- Notes field for editors

---

## PHASE 13: FIFA, SPORTS & LIVE DATA

**SKILL: Use `live-data-widget-integration-skill`**

### Task 13.1: Wire FIFA World Cup scores

The `sports.ts` file has football-data.org integration with a mock fallback.

**Fix:**
- If `FOOTBALL_API_KEY` is set, show real FIFA World Cup scores
- If not set, create a MANUAL score updater in admin where editors can enter scores
- Create a dedicated "FIFA World Cup 2026" sports page

### Task 13.2: Create sports hub page

Create `/sports` hub page:
- Live scores section (cricket + football)
- Latest sports articles
- Upcoming fixtures
- League tables (cricket + football)

### Task 13.3: Create NEPSE market page with real data

- Build a proper NEPSE page at `/nepse`
- Show market summary (index, change, turnover)
- Top gainers/losers
- Sector-wise performance
- Use the admin to input manual data if API is unreliable

---

## PHASE 14: INFRASTRUCTURE & PRODUCTION READINESS

### Task 14.1: Fix JSON store for production

The JSON store writes to `process.cwd() + '/data/articles.json'`. This is READ-ONLY on Vercel.

**Fix:**
- Add `ARTICLES_STORE_URL` env var for blob storage
- Implement read/write adapters: local file for dev, HTTP/blob for production
- Remove the silent catch block — surface errors

### Task 14.2: Implement cache purge

The Cloudflare cache purge is a TODO stubs.

**Fix:** Implement at minimum a basic cache purge mechanism, or document that this is deferred.

### Task 14.3: Fix or hide PWA manifest

The layout references `/manifest.webmanifest` but the file doesn't exist.

**Fix:** Either create the manifest file or remove the reference.

### Task 14.4: Run production audits

Run ALL audit scripts:
- `pnpm audit:public` — scan for mock/demo artifacts
- `pnpm audit:ads` — check ad placements
- `pnpm audit:architecture` — check invariants
- `pnpm launch:gate` — verify launch readiness

Fix ALL failures.

### Task 14.5: Clean up env vars

- Add `BETTER_AUTH_URL` to `.env` and `.env.example`
- Add `ARTICLES_STORE_URL` to `.env.example`
- Document all missing env vars from code that aren't in `.env.example`
- Remove `NEXT_PUBLIC_SITE_URL` duplicate from `.env`

---

## PHASE 15: COMPREHENSIVE CODE QUALITY

### Task 15.1: Fix type safety issues

- Replace `as any` and `as string` casts with proper types
- Remove non-null assertions where possible
- Fix empty catch blocks
- Fix `void session` pattern in admin pages

### Task 15.2: Remove dead code

- Remove `apps/web/lib/live-data.ts` (old mock system)
- Remove `mini-services/` (empty placeholder)
- Remove `db/` (empty directory)
- Remove `upload/` (empty directory)
- Remove `.zscripts/` (empty directory)

### Task 15.3: Unify the two live data systems

Ensure ALL live widgets use the `lib/live/` system. Remove any remaining references to `lib/live-data.ts`.

### Task 15.4: Add proper error handling

- Replace empty catch blocks with at minimum `console.error`
- Add proper error boundaries on client components
- Surface DB connection errors instead of swallowing them

---

## PHASE 16: SEO & GOOGLE NEWS

**SKILL: Use `news-seo-google-news-skill`, `schema`, `seo-audit`**

### Task 16.1: Create trust pages with real content

- `/about` — real about page with mission, team, contact
- `/team` — list actual editorial team members
- `/ethics` — complete ethics policy
- `/privacy` — complete privacy policy
- `/contact` — ADD A CONTACT FORM (not just mailto link)
- `/editorial-policy` — complete policy
- `/corrections-policy` — complete policy
- `/fact-check-policy` — complete policy

### Task 16.2: Fix canonical URLs

All canonical URLs should use the production domain, not `http://localhost:3000`

### Task 16.3: Verify sitemap, RSS, robots.txt

- Check `/sitemap.xml` returns valid XML
- Check `/news-sitemap.xml` returns valid XML  
- Check `/rss.xml` returns valid RSS
- Check `/robots.txt` is correct
- Fix any broken outputs

### Task 16.4: Add NewsArticle JSON-LD

Verify every article page has proper `NewsArticle` schema with all required fields.

---

## FINAL TASK: SELF-AUDIT — FIND WHAT I MISSED

After completing ALL tasks above, do a comprehensive self-audit:

**Scan for:**
1. Any remaining "Coming soon", "TODO", "FIXME", "HACK", "TEMP" in source code
2. Any remaining `console.log` or `debugger` statements
3. Any remaining mock data that should be real
4. Any broken links (click through every page)
5. Any missing loading/error/empty states
6. Any pages that look incomplete or unfinished
7. Any UI that fails the "anti-slop" test
8. Any routes referenced in navigation that don't exist
9. Any API endpoints needed but not created
10. Any env vars referenced in code but not documented

**Produce a file `FINAL_AUDIT.md` listing:**
- Every issue you found that I didn't mention
- Every issue from the original prompt that you fixed
- Every issue you couldn't fix (with reason)
- A CONTINUATION PROMPT for the next session

---

## SKILLS YOU MUST USE

The project has these custom skills. Load them before relevant tasks:

| Skill | When to use |
|-------|-------------|
| `live-data-widget-integration-skill` | All live data tasks (weather, AQI, NEPSE, sports, forex, gold, etc.) |
| `moderation-and-trust-safety-skill` | Reader submissions, comments, fact-checking, moderation |
| `news-seo-google-news-skill` | SEO, sitemaps, schema, Google News, trust pages |
| `newsroom-cms-architecture-skill` | All admin/CMS changes, roles, permissions, workflow |

Plus these built-in universal skills:

| Skill | When to use |
|-------|-------------|
| `impeccable` | Any UI design or redesign work |
| `design-anti-slop` | Reviewing and fixing generic AI patterns in UI |
| `karpathy-guidelines` | ALL coding — think before coding, simplicity first, surgical changes |
| `agentic-engineering` | Breaking down work, eval-first execution |
| `incremental-implementation` | Multi-file changes — deliver in thin slices |
| `verification-before-completion` | Before claiming ANY task is done — run verification first |
| `secure-code-guardian` | Auth, login, security-sensitive tasks |
| `form-expert` | Any form creation (login, register, submit, settings) |
| `fullstack-guardian` | Full-stack features connecting frontend to data |
| `analytics` | Analytics dashboard, event tracking |
| `caching-strategies` | Data caching, live data caching |
| `paywalls` | Premium content, subscription UI |
| `signup` | Registration flow |
| `onboarding` | Reader onboarding |
| `accessibility-audit` | WCAG compliance |
| `tailwind-css` | CSS/styling work |
| `nextjs-developer` | Next.js architecture decisions |
| `react-expert` | React component design |
| `seo-audit` | SEO tasks |
| `schema` | Structured data |
| `error-handling` | Error boundaries, error responses |

---

## VERIFICATION GATE

Before claiming ANY task is complete:

1. Run `pnpm typecheck` — fix ALL type errors
2. Run `pnpm lint` — fix ALL lint errors
3. Run `pnpm test` — fix ALL test failures
4. Run `pnpm build` or `pnpm --filter @nagarikwatch/web build` — fix ALL build errors
5. Run `pnpm verify:static` if available
6. **Manually test the feature** — don't just assume it works

---

## DELIVERABLES

By the end of this session, produce:

1. **A fully working site** — all pages functional, all buttons clickable, all data flowing
2. **FINAL_AUDIT.md** — comprehensive audit of what was done, what was found, what remains
3. **CONTINUATION_PROMPT.md** — a prompt for the next session to complete anything remaining
4. **All code committed** with descriptive commit messages

---

## GOOD LUCK. MAKE IT PRODUCTION-GRADE. NO HALF MEASURES.
