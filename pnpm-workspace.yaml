packages:
  # Canonical deployable applications. Keep these explicit so a stale legacy
  # apps/cms directory cannot silently re-enter the pnpm workspace.
  - 'apps/web'
  - 'apps/admin'
  - 'packages/*'
