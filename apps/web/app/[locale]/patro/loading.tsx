import { PatroDeskSkeleton } from '@nagarikwatch/ui'

/** पात्रो soft-nav — today banner + widgets + grid bones. */
export default function Loading() {
  return (
    <div className="mx-auto max-w-page px-3 py-4 sm:px-4 sm:py-5">
      <PatroDeskSkeleton />
    </div>
  )
}
