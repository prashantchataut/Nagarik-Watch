'use client'

/**
 * @deprecated Replaced by TopicsStrip in the Ratopati-led masthead.
 * Kept so accidental imports fail loudly in review; do not use.
 */
export function SecondaryNav(_props: { locale: unknown }) {
  return null
}
